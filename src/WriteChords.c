#include "WriteChords.h"

//Window *window;



int main(void)
{
  //init(); //putting this in
  
  //window = window_create();
  // SETUP
  setup_chord_type_menu();
  // LOOP
  app_event_loop();

  // CLEANUP
 // cleanup_chord_menu();
  
  //deinit(); //also putting this in
}
